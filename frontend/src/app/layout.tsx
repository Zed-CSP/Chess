import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Chess Master - Play Chess Online',
  description: 'A modern chess platform with AI opponents, puzzles, and tournaments',
  keywords: ['chess', 'online chess', 'chess puzzles', 'chess AI', 'tournaments'],
  authors: [{ name: 'Chess Master Team' }],
  viewport: 'width=device-width, initial-scale=1',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <div id="root">
          {children}
        </div>
      </body>
    </html>
  )
} 